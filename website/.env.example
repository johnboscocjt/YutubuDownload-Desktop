# Vercel KV (Storage → KV in Vercel dashboard → Connect to project)
# Tracks site-hosted .deb downloads and terminal install clicks
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=

# Without KV, localhost saves counts to website/.data/download-stats.json (persists across restarts)
