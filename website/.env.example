# Optional: starting count shown before real tracking begins
NEXT_PUBLIC_STATS_BASELINE=1284

# Vercel KV (Storage → KV in Vercel dashboard → Connect to project)
# Enables live download counter increments when users click Download
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=
